# PDF Table Extractor

This script extracts tables from a PDF file and saves them into an Excel file.

## Requirements

- Python 3.x
- `tabula-io`
- `pandas`

## Installation

Install the required Python packages using pip:

```bash
pip install -r requirements.txt
blabla